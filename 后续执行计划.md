# gei-world 后续执行计划

## 1. 当前基线

当前项目已经从演示型半成品推进到一个较稳定的单机原型。

最近完成的关键改动：

- 使用项目本地 Conda 环境：`.conda`
- 完善 `.gitignore`，避免环境、缓存、测试产物污染提交
- 测试统一迁移到 `tests/`，并通过 `pytest.ini` 固定测试入口
- 修复胜负判定：玩家同时失去城市和单位后才算淘汰
- 打通基础攻城流程：士兵进入敌方城市中心后可触发城市和领土易主
- 实现存档加载核心 round-trip：地图、玩家、城市、单位、回合状态可恢复
- UI 增加消息栏，操作反馈不再只依赖控制台
- UI 中 AI 玩家可在玩家结束回合后自动行动

当前验证命令：

```bash
./.conda/bin/python -m pytest
```

当前测试基线：

```text
45 passed
```

后续所有 Python 命令必须使用项目 Conda 环境执行：

```bash
./.conda/bin/python
```

## 2. 总体推进原则

短期目标不是继续堆新单位、科技树或网络多人，而是先把基础闭环做扎实：

1. 引擎规则可信
2. 战斗/攻城结果可解释
3. 存档可继续游玩
4. UI 流程完整
5. AI/地图/移动逐步提升

每一轮改动都应满足：

- 先补或同步补测试
- 只做一个明确主题
- 使用 `.conda` 环境运行测试
- 通过后再提交
- commit message 使用裸类型前缀，例如 `fix:` / `feat:` / `chore:`，不加 scope 括号

## 3. 下一阶段优先事项

### 优先级 1：引擎规则硬化（已完成第一轮）

这是下一步最优先事项。原因是 UI、AI、存档都依赖 `GameEngine` 的规则权威性。

当前状态：已完成第一轮硬化，覆盖归属校验、敌方地块/敌方城市核心范围建城限制、移动/战斗/攻城后即时胜负检查。

当前风险：

- `execute_action()` 只校验 `action.player_id` 是否是当前玩家
- 具体单位、城市对象的归属校验还不够严格
- 理论上可以构造非法 action 操作别人的单位或城市

建议补测试并修复：

- 非当前玩家不能行动
- 当前玩家不能移动别人的单位
- 当前玩家不能用别人的移民建城
- 当前玩家不能用别人的城市生产单位
- 建城不能建在敌方地块上
- 建城不能建在敌方城市核心范围内
- 战斗/攻城后统一检查胜负状态

主要文件：

- `src/game_engine.py`
- `src/systems/city_system.py`
- `tests/test_gameplay_rules.py`

验收标准：

- 新增非法行动回归测试
- 所有非法行动返回失败且不改变游戏状态
- `./.conda/bin/python -m pytest` 全部通过

### 优先级 2：引入结构化行动结果（已完成第一轮）

当前状态：已完成第一轮结构化行动结果，新增 `ActionEvent` / `ActionResult`，`GameEngine.execute_action_with_result()` 可返回基础 message/events，UI 已开始使用引擎结果驱动消息栏。

建议逐步引入：

```python
ActionResult(
    success: bool,
    message: str,
    events: list
)
```

可以先保留兼容层，避免一次性大改。

建议事件类型：

- `unit_moved`
- `unit_destroyed`
- `combat_resolved`（已完成第一轮）
- `tile_occupied`
- `city_built`
- `city_captured`
- `unit_produced`
- `player_eliminated`
- `game_over`

主要文件：

- `src/models.py`
- `src/game_engine.py`
- `src/systems/combat_system.py`
- `ui/client_controller.py`
- `tests/test_gameplay_rules.py`

验收标准：

- UI 消息栏可以显示引擎返回的真实结果
- 战斗、攻城、淘汰、胜利都有明确事件
- 旧有测试通过，并新增事件相关测试

### 优先级 3：存档系统产品化（已完成第一轮）

当前状态：已完成第一轮产品化，存档可保存/恢复 `winner_id` 与 AI 玩家配置，增强演示已更新旧加载提示，`GameStateSerializer` 的静默反序列化占位已改为显式异常。

待补内容：

- 保存/恢复 `winner` id
- 保存/恢复 AI 玩家配置
- 保存/恢复真正的 explored tiles（已完成）
- UI 增加加载存档入口
- 加载后继续游戏、继续 AI 回合
- 删除或更新旧的“保存系统尚未完全实现”提示
- 统一 `src/utils.py` 中的序列化占位，避免和 `save_system.py` 概念重复

主要文件：

- `src/systems/save_system.py`
- `src/utils.py`
- `ui/client_controller.py`
- `enhanced_demo.py`
- `tests/test_gameplay_rules.py`

验收标准：

- UI 可保存并加载继续游玩
- 加载后当前玩家、AI 配置、胜负状态、视野状态正确
- 保存测试使用临时目录，不污染 `saves/`

### 优先级 4：补 UI 完整游戏流程（已完成第一轮）

当前状态：已完成第一轮 UI 流程补强，增加游戏结束面板，城市生产成本改为读取 `UNIT_CONFIG`，并补充结束面板渲染测试。

建议补：

- 开始菜单：新游戏、加载游戏、退出
- 新游戏参数：玩家数、AI 类型、地图类型、种子
- 游戏结束面板
- 选中单位详情
- 城市详情
- 合法移动范围高亮
- 战斗/攻城详情展示
- 城市生产成本从 `src/config.py` 读取，去掉 UI 硬编码

主要文件：

- `ui/client_controller.py`
- `ui/systems/ui_system.py`
- `ui/systems/render_system.py`
- `src/config.py`

验收标准：

- 玩家不看控制台也能完成一局
- 游戏开始、保存、加载、结束都有 UI 流程
- UI 中没有重复硬编码核心规则参数

### 优先级 5：提升 AI、地图和移动质量（已完成第一轮）

当前状态：已完成第一轮出生点公平性改进，初始化会优先选择不重叠且尽量分散的陆地出生点，并已补充 4 玩家出生点唯一性/间距测试。

建议补：

- 玩家出生点公平分配
- 保证出生点可达，或引入海军/渡海规则
- 单位移动改为路径校验，而不是只看六边形距离
- AI 使用路径寻路
- AI 能识别敌方城市并组织多士兵攻城
- AI 按扩张、经济、军事阶段切换策略
- 视野系统维护真正的 explored 历史（已完成）

主要文件：

- `src/systems/ai_system.py`
- `src/systems/map_system.py`
- `src/systems/map_generators.py`
- `src/systems/unit_system.py`
- `src/systems/vision_system.py`

验收标准：

- 出生点不会重叠或明显不公平
- 单位不能跨海或穿越不可达地块跳跃
- AI 能完成基本扩张和攻城目标

## 4. MVP 可玩性收口计划

AI 逻辑先暂停优化，下一阶段目标改为把当前原型收口成“别人也能试玩”的单机 MVP。短期不扩新地形、新单位、科技树、建筑或复杂 AI，优先消除 UI 主流程和交互可靠性风险。

### 阶段 4.1：UI 操作可靠性硬化（已完成第一轮）

目标：保证玩家看到的六边形、鼠标点击拾取、摄像机缩放/拖动后的坐标完全一致，避免“点 A 格选中 B 格”。

当前状态：已完成第一轮坐标系统硬化。

已处理：

1. 修正 `HexRenderer.pixel_to_hex()` 与 `hex_to_pixel()` 的互逆关系
2. 统一 `OFFSET_X` / `OFFSET_Y` 的语义：渲染使用偏移后的世界坐标，拾取使用相同偏移反算
3. 统一 `RenderSystem` 和 `CameraSystem` 的 world/screen 坐标原点约定
4. 补充严格坐标回归测试：hex/pixel 往返、offset 往返、camera/render 一致性、渲染中心点击拾取一致性
5. 已运行：

```bash
./.conda/bin/python -m pytest
```

当前结果：

```text
44 passed
```

### 阶段 4.2：主菜单与结束后重开流程（已完成第一轮）

目标：玩家不依赖控制台即可开始、加载、退出和重新开始。

当前状态：已完成第一轮主流程补强。

已处理：

- 开始界面显示 `SPACE` 新游戏、`L` 加载游戏、`ESC` 退出
- 开始界面可展示加载失败等最近反馈消息
- 游戏结束面板显示胜者，并支持 `R` 重新开始、`ESC` 退出
- 新游戏和重开使用全新 `GameEngine`，避免旧局状态残留
- 新游戏、加载、结束回合、取消选择时统一清理选中状态、城市面板和可达高亮
- 关闭城市面板时清理临时按钮，避免隐藏按钮继续响应点击
- 已补充开始菜单、加载、游戏结束重开/退出和加载后状态清理回归测试

### 阶段 4.3：新手提示与失败原因补强

目标：玩家知道自己该做什么，以及为什么操作失败。

最小范围：

- 首次进入显示基础操作提示
- 选中移民/士兵/城市时显示下一步建议
- 移动失败、建城失败、生产失败时 UI 显示具体原因
- 全程不要求查看控制台

### 阶段 4.4：MVP 验收清单与说明文档

目标：形成一份可重复验收的试玩流程。

最小范围：

- 更新 `使用说明.md` 或新增 MVP 验收清单
- 覆盖启动、新游戏、建城、生产、移动、保存、加载、战斗/攻城、胜负
- 记录当前已知限制

## 5. 后续工作约定

后续开发时优先参考本文件，除非新的实际代码状态或用户指令改变优先级。

每轮开始前先确认：

- 当前工作区是否干净
- 最新测试是否通过
- 本轮改动是否对应本文中的一个明确阶段

每轮结束时输出：

- 改了什么
- 测试结果
- 是否已提交
- 下一步建议
