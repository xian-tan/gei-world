# 六边形策略游戏 - 使用说明

## 📖 项目简介

这是一个基于Python开发的**六边形蜂窝地图多人策略游戏**，采用回合制玩法。玩家通过建立城市、生产单位、扩张领土来与其他玩家竞争。游戏特色包括战争迷雾、AI对手、多种地图生成算法等。

### 🎯 游戏目标
- 建立和发展城市
- 生产移民和士兵
- 扩张领土控制更多资源
- 击败其他玩家获得胜利

## 🏗️ 技术架构

### 设计理念
- **逻辑与渲染分离** - 纯逻辑引擎，不依赖UI
- **模块化设计** - 8个独立系统，职责明确
- **可扩展架构** - 支持添加新功能和AI策略
- **数据可序列化** - 支持游戏保存和加载

### 核心系统
```
GameEngine (游戏引擎)
├── MapSystem          # 六边形地图管理
├── PlayerSystem       # 玩家数据管理
├── UnitSystem         # 单位（移民/士兵）管理
├── CitySystem         # 城市建设与生产
├── CombatSystem       # 战斗与占领系统
├── VisionSystem       # 战争迷雾与视野
├── TurnSystem         # 回合流程控制
├── AISystem           # AI玩家策略
└── SaveSystem         # 游戏保存/加载
```

## 🎮 游戏机制

### 基础概念

#### 六边形地图
- 使用轴向坐标系 `(q, r)`
- 每个六边形有6个相邻位置
- 支持陆地和海洋地形

#### 单位类型
- **移民 (Settler)** - 用于建立新城市
- **士兵 (Soldier)** - 用于战斗和占领领土

#### 城市系统
- 城市建立后自动获得周围领土
- 可生产新单位（消耗金币）
- 提供经济收入来源

#### 经济系统
- 每块领土每回合产生1金币收入
- 建造移民需要50金币
- 建造士兵需要30金币

### 战斗规则
- 数量对比制：单位多的一方获胜
- 失败方单位全部消失
- 胜利方剩余单位 = 原数量 - 敌方数量
- 城市有防御值，需要足够兵力才能攻占

### 视野系统
- 每个单位有视野范围
- 玩家只能看到单位视野内和自己领土
- 未知区域显示战争迷雾

## 📁 项目结构

```
gei-world/                     # 项目根目录
├── 使用说明.md                # 本文档
├── README.md                  # 原始需求文档
├── PROJECT_STATUS.md          # 项目状态报告
├── requirements.txt           # Python依赖
│
├── src/                       # 核心代码
│   ├── config.py             # 游戏配置参数
│   ├── models.py             # 数据模型定义
│   ├── game_engine.py        # 主游戏引擎
│   ├── utils.py              # 实用工具函数
│   └── systems/              # 游戏系统模块
│       ├── map_system.py     # 地图系统
│       ├── player_system.py  # 玩家系统
│       ├── unit_system.py    # 单位系统
│       ├── city_system.py    # 城市系统
│       ├── combat_system.py  # 战斗系统
│       ├── vision_system.py  # 视野系统
│       ├── turn_system.py    # 回合系统
│       ├── ai_system.py      # AI系统
│       ├── save_system.py    # 保存系统
│       └── map_generators.py # 地图生成器
│
├── tests/                     # 测试文件
│   ├── test_basic.py         # 基础功能测试
│   └── test_improvements.py  # 增强功能测试
│
├── saves/                     # 游戏存档目录
├── demo.py                   # 基础演示程序
├── interactive_game.py       # 交互式游戏
├── enhanced_demo.py          # 增强演示（AI+保存）
└── test_enhanced_features.py # 功能验证程序
```

## 🚀 快速开始

### 1. 环境准备

```powershell
# 确保Python 3.8+已安装
python --version

# 安装依赖（目前只需pytest用于测试）
pip install pytest

# 或安装所有依赖
pip install -r requirements.txt
```

### 2. 验证安装

```powershell
# 运行基础测试
python -m pytest tests/ -v

# 运行功能验证
python test_enhanced_features.py
```

### 3. 体验游戏

#### 🎯 选择一：基础演示（5分钟）
```powershell
python demo.py
```
- 自动运行简单的AI对战
- 展示基本游戏流程
- 适合快速了解游戏机制

#### 🎮 选择二：交互式游戏（推荐）
```powershell
python interactive_game.py
```
- 完整的命令行游戏体验
- 支持人类玩家操作
- 可自定义玩家数量和地图

#### 🤖 选择三：增强演示（AI + 保存功能）
```powershell
python enhanced_demo.py
```
- 多种游戏模式选择
- AI对战观战模式
- 游戏保存和加载功能

## 🎯 详细使用指南

### 交互式游戏操作

#### 游戏初始化
1. 设置玩家数量（2-4人）
2. 输入玩家名称
3. 选择地图类型：
   - **标准地图** - 柏林噪声生成，真实地形
   - **岛屿地图** - 多个独立岛屿
   - **随机地图** - 完全随机地形
4. 设置随机种子（可选，用于重现地图）

#### 玩家回合操作
每个玩家回合可以执行：

1. **查看单位** - 显示所有单位位置和状态
2. **移动单位** - 选择单位和目标位置
3. **建立城市** - 使用移民建城
4. **城市生产** - 在城市中建造新单位
5. **查看地图信息** - 显示地图统计
6. **结束回合** - 切换到下一玩家

#### 游戏获胜条件
- 消灭所有敌方城市
- 成为唯一拥有城市的玩家

### AI系统使用

#### AI类型
- **简单AI (Simple)** - 平衡发展策略
  - 优先建城扩张
  - 适度军事建设
  - 适合新手对战
  
- **激进AI (Aggressive)** - 军事导向策略
  - 大量建造士兵
  - 主动攻击敌方
  - 提供挑战性对战

#### 添加AI玩家
```python
from src.systems.ai_system import AIManager

ai_manager = AIManager()
ai_manager.add_ai_player(player, "simple", "easy")    # 简单AI
ai_manager.add_ai_player(player, "aggressive", "hard") # 激进AI
```

### 保存系统使用

#### 保存游戏
```python
from src.systems.save_system import GameSaveSystem

save_system = GameSaveSystem()
save_system.save_game(engine, "我的游戏")
```

#### 查看保存文件
```python
saves = save_system.list_saves()
for save in saves:
    print(f"{save['name']} - 回合{save['turn']}")
```

#### 加载游戏
```python
loaded_engine = save_system.load_game("我的游戏")
# 注意：完整加载功能正在开发中
```

## ⚙️ 配置自定义

### 修改游戏参数

编辑 `src/config.py` 文件：

```python
# 地图配置
MAP_CONFIG = {
    "radius": 10,           # 地图大小（半径）
    "ocean_ratio": 0.7,     # 海洋比例
    "seed": None,           # 固定随机种子
}

# 玩家配置
PLAYER_CONFIG = {
    "initial_gold": 100,    # 初始金币
    "max_players": 4,       # 最大玩家数
}

# 单位配置
UNIT_CONFIG = {
    "soldier_movement": 2,  # 士兵移动力
    "soldier_vision": 2,    # 士兵视野
    "settler_cost": 50,     # 移民成本
    "soldier_cost": 30,     # 士兵成本
}
```

### 创建自定义地图生成器

```python
from src.systems.map_generators import MapGenerator

class MyMapGenerator(MapGenerator):
    def generate(self):
        # 实现自定义地图生成逻辑
        pass
```

### 开发自定义AI策略

```python
from src.systems.ai_system import AIPlayer

class MyAI(AIPlayer):
    def get_action(self, engine):
        # 实现自定义AI决策逻辑
        pass
```

## 🔧 开发和扩展

### 运行测试
```powershell
# 运行所有测试
python -m pytest tests/ -v

# 运行特定测试
python -m pytest tests/test_basic.py -v

# 运行增强功能验证
python test_enhanced_features.py
```

### 代码结构理解

#### 游戏行动系统
所有游戏操作都通过 `GameAction` 对象：
```python
action = GameAction(
    player_id="player_1",
    action_type=ActionType.MOVE_UNIT,
    params={"unit_id": "unit_123", "target": [2, -1]}
)
success = engine.execute_action(action)
```

#### 坐标系统
使用六边形轴向坐标：
```python
coord = HexCoord(q=2, r=-1)
neighbors = coord.neighbors()  # 获取6个相邻坐标
distance = coord1.distance_to(coord2)  # 计算距离
```

### 添加新功能建议

1. **新单位类型**
   - 在 `UnitType` 枚举中添加类型
   - 在 `UnitSystem` 中添加创建逻辑
   - 更新 `UNIT_CONFIG` 配置

2. **新建筑类型**
   - 扩展城市系统
   - 添加建筑效果逻辑
   - 更新UI显示

3. **科技系统**
   - 创建新的 `TechSystem`
   - 添加科技树数据结构
   - 集成到游戏引擎

## 🐛 常见问题

### Q: 游戏运行报错怎么办？
A: 确保Python 3.8+，检查所有文件是否完整，运行测试验证安装。

### Q: 如何修改地图大小？
A: 编辑 `src/config.py` 中的 `MAP_CONFIG["radius"]` 参数。

### Q: AI玩家太简单了怎么办？
A: 可以使用"aggressive"类型AI，或参考`ai_system.py`开发更复杂的策略。

### Q: 保存的游戏加载不了？
A: 当前加载功能还在完善中，保存功能正常，加载功能在下个版本实现。

### Q: 如何添加更多玩家？
A: 修改 `PLAYER_CONFIG["max_players"]` 参数，但建议不超过6人以保持游戏平衡。

## 🚀 未来计划

### 第二阶段：UI系统
- [ ] pygame可视化界面
- [ ] 六边形地图渲染
- [ ] 鼠标点击操作
- [ ] 动画效果

### 功能扩展
- [ ] 完整的保存/加载系统
- [ ] 网络多人游戏
- [ ] 更多AI策略
- [ ] 科技树系统
- [ ] 建筑系统
- [ ] 地形效果

### 性能优化
- [ ] 大地图优化
- [ ] AI决策速度提升
- [ ] 内存使用优化

## 📞 技术支持

如果您在使用过程中遇到问题：

1. 查看 `PROJECT_STATUS.md` 了解项目状态
2. 运行测试确认环境正常
3. 检查 `src/config.py` 配置是否合理
4. 参考示例程序学习用法

---

**🎮 祝您游戏愉快！这个框架为您提供了一个完整的策略游戏基础，您可以根据需要进行定制和扩展。**
